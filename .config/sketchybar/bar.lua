local colors = require("colors")

-- Equivalent to the --bar domain
sbar.bar({
	height = 36,
	color = 0xbb000000,
	padding_right = -10,
	padding_left = -15,
	margin = 8,
	corner_radius = 8,
	y_offset = 8,
	font_smoothing = false,
})
